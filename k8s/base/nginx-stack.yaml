apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-php
  namespace: www-server
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  replicas: 3
  selector:
    matchLabels:
      app: server-www-php
  template:
    metadata:
      labels:
        app: server-www-php
    spec:
      imagePullSecrets:
        - name: ghcr-secret 
      containers:

      - name: nginx-deplo
        image: ghcr.io/1xxxxxxx4/blog-kubernetes-nginx:latest
        imagePullPolicy: Always
        ports:
          - containerPort: 80
        
      - name: php-deplo
        image: ghcr.io/1xxxxxxx4/blog-kubernetes-php:latest
        imagePullPolicy: Always
        ports:
          - containerPort: 9000
        env:
          - name: DB_USER
            valueFrom: 
              secretKeyRef:
                name: pg-app-secret
                key: username
          
          - name: DB_PASSWORD
            valueFrom: 
              secretKeyRef:
                name: pg-app-secret
                key: password

          - name: DB_NAME
            valueFrom:
              secretKeyRef:
                name: pg-app-secret
                key: database